namespace Scheduler
{
    public enum ParkingData
    {
        ParkingId,
        UserId,
        Logger
    }

    public enum Jobs
    {
        AutoStopParking
    }

    public enum Triggers
    {
        AutoStopParking
    }
}